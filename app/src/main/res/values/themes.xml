<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EcoGrid" parent="android:Theme.Light.NoTitleBar">
        <!-- Customize your theme here. -->
    </style>
    
    <!-- Google Fonts provider certificates are referenced from
         com.google.android.gms: they should not be copied locally. -->
    
    <!-- Splash screen theme -->
    <style name="Theme.EcoGrid.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_ecogrid_logo_large</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.EcoGrid</item>
    </style>
</resources>